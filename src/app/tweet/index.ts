import {types} from './types';
import { query } from  './queries'; 
import { muatations}  from './muatations';
import { resolvers}  from './resolvers';

export const Tweet = {types, muatations, resolvers, query };